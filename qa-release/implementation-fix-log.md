# Implementation Fix Log

| Fix ID | Date | Description | Files Changed |
|--------|------|-------------|---------------|
| FIX-321 | 2026-06-22 | Operation Profile won't save (founder-reported). ROOT CAUSE: the property **edit wizard** (`[id]/edit/page.tsx` inline `StepTypeProfile`) stored the DISPLAY LABEL ("Serviced Accommodation") as the `operation_profile` value, but the canonical enum + DB `template` are snake_case (`serviced_accommodation`). Effects: (a) the selected profile button never highlighted (label !== stored snake_case) so the field looked uneditable; (b) saving wrote an invalid `template` enum → the update was rejected. Fix: profile options now derive from `PLANNING_PROFILES` ({key,label}) so the VALUE is the canonical enum key; `defaultValues`/`reset` use `long_term_let`; the Review step maps value→label for display. Dependent field: selecting a profile now auto-derives the obvious `property_type` (hmo→hmo, commercial→commercial, mixed_use→mixed_use). Also made the detail-page inline editor (`OverviewTab`) profile list derive from `PLANNING_PROFILES` (was a hardcoded subset missing refinancing/dev_flip/co_living — values there were already correct). The create wizard (`StepProfile`) already used canonical keys (unaffected). | `src/app/(app)/app/portfolio/properties/[id]/edit/page.tsx`, `src/components/portfolio/property-detail/OverviewTab.tsx` |
| FIX-320 | 2026-06-22 | Booking checkout — branding + overflow + enrichment (all 4 flows). (1) Branding: the secure-checkout chrome header now shows the real Propvora logo image (was a plain text wordmark). (2) Overflow/wrap: order-summary line items, add-ons and meta rows now use `items-start` + `min-w-0 break-words` on labels and `shrink-0` on prices/values so long labels (e.g. "£X × N nights", long postcodes/coverage lists) wrap cleanly instead of overflowing the rail. (3) Enrichment: `MarketplaceCheckoutConfig` gained `included` (What's included), `trustChips` (verified/insured/rating/jobs), `policyNotes` (Good to know) and `whatNext` (numbered reassurance timeline) — rendered in the summary rail + details step. All four checkout configs populated from real data: stays (amenities, host, cancellation), services (package includes + deliverables + jobs done), supplier (call-out scope, certs, insurance), emergency (dispatch, police-vetted, 999 notice). | `src/features/checkout/components/CheckoutShell.tsx`, `src/components/checkout/MarketplaceCheckout.tsx`, `src/app/stays/[slug]/checkout/page.tsx`, `src/app/(app)/app/marketplace/suppliers-hub/services/[slug]/book/page.tsx`, `src/app/(app)/app/marketplace/suppliers-hub/[slug]/book/page.tsx`, `src/app/(app)/app/marketplace/suppliers-hub/emergency/[slug]/book/page.tsx` |
| FIX-319 | 2026-06-22 | Save login-gate on inline card hearts. The marketplace card hearts (StayCard, ProviderCard, ProviderMapCard, ServiceOfferCard, EmergencyServiceCard) toggled localStorage favourites with no auth check — anonymous visitors "saved" into nothing. New shared `save-guard.ts` resolves auth state ONCE at module scope (one `getUser()` for a whole grid of cards) and, when signed out, redirects to `/login?next=<url>` before any toggle. All five card heart handlers are now async and bail through the guard. StayCard additionally now persists favourites to `localStorage` (`propvora_saved_stays`) like the others (was in-memory only). Complements FIX-317 (ShareSaveButtons). | `src/lib/public-marketplace/save-guard.ts` (NEW), `src/components/public-marketplace/cards/StayCard.tsx`, `src/components/public-marketplace/cards/ProviderCard.tsx`, `src/components/public-marketplace/cards/ProviderMapCard.tsx`, `src/components/public-marketplace/cards/ServiceOfferCard.tsx`, `src/components/public-marketplace/cards/EmergencyServiceCard.tsx` |
| FIX-318 | 2026-06-22 | Marketplace filter enrichment — the public stays/services/suppliers/emergency filter chips were dead labels ("Price ↑", "Type ↓", "Bedrooms ↓") that toggled a boolean with no options. New shared premium `FilterBar` component (dependency-free): dual-thumb **range slider** (price/response-time), **multi-select** checklist (type/trade/category), **stepper** (bedrooms/bathrooms/min-rating), **toggle** chips (pets/instant/verified/short/long, vetted/insured/pro/24-7, etc.) and a sort menu. Each chip opens a portalled, anchored popover (escapes overflow/sticky clipping) with Reset/Apply, live chip summaries (e.g. "Price £50–£200"), an active-count badge and Clear-all. Schemas are **data-driven** (price bounds + present types/trades/categories derived from the actual results). Wired into: Stays list (`StaysFilterClient`), Stays map (also made area chips data-driven + live result count, replacing hardcoded "1,248"), Services/Suppliers/Emergency list (`ServicesFilterClient`, per-tab schema — previously had NO filters), Services map (replaced single-select chips + wired sort). | `src/components/public-marketplace/FilterBar.tsx` (NEW), `src/app/stays/StaysFilterClient.tsx`, `src/app/stays/map/page.tsx`, `src/app/services/ServicesFilterClient.tsx`, `src/app/services/map/page.tsx` |
| FIX-304 | 2026-06-21 | Marketplace checkout & booking — end-to-end premium multi-step checkout for stays, services, suppliers, emergency. New generic `MarketplaceCheckout` engine (Details → Payment → Review/confirmation; modelled payment, order summary, extras, promo, VAT, deposit, reference number) reusing the shared `CheckoutShell`/primitives. New reusable `ShareSaveButtons` (Web Share API → clipboard fallback + localStorage favourites) wired into stays/service/supplier profile headers. New reactive `ServiceBookingPanel` — tier selector (Basic/Standard/Premium) drives price + includes + Book CTA in the service sidebar. New routes: `/stays/[slug]/checkout`, `services/[slug]/book`, `[slug]/book` (supplier), `emergency/[slug]/book`. Rewired all profile CTAs (Book Now / Choose tier / Add-on / Book Emergency) away from the dead `work/jobs/new` link to the new checkout. | `src/components/checkout/MarketplaceCheckout.tsx` (NEW), `src/components/checkout/ShareSaveButtons.tsx` (NEW), `src/components/checkout/ServiceBookingPanel.tsx` (NEW), `src/app/stays/[slug]/checkout/page.tsx` (NEW), `src/app/(app)/app/marketplace/suppliers-hub/services/[slug]/book/page.tsx` (NEW), `src/app/(app)/app/marketplace/suppliers-hub/[slug]/book/page.tsx` (NEW), `src/app/(app)/app/marketplace/suppliers-hub/emergency/[slug]/book/page.tsx` (NEW), `src/app/(app)/app/marketplace/suppliers-hub/services/[slug]/page.tsx`, `src/app/(app)/app/marketplace/suppliers-hub/[slug]/page.tsx`, `src/app/(app)/app/marketplace/suppliers-hub/emergency/[slug]/page.tsx`, `src/app/stays/[slug]/page.tsx` |
| FIX-316 | 2026-06-22 | Marketplace enrichment + nav merge. Public + PM marketplace looked broken/sparse — the queries were either/or (returned ONLY the 1 live demo supplier and ignored the 12-item seed). Now `getPublicProviders/Offers/EmergencyServices` MERGE live + seed (deduped by slug) so both surfaces are rich (and stay aligned since they share queries). Merged the redundant "Suppliers" + "Services" public nav entries (header + footer) into one "Suppliers & Services" → /services (they're one merged surface; /suppliers redirects to /services). | `src/lib/public-marketplace/queries.ts`, `src/components/marketing/PublicNav.tsx`, `src/components/marketing/PublicFooter.tsx` |
| FIX-315 | 2026-06-22 | Public/property polish batch (browser-found). (1) Auth-aware public nav (`PublicNav`): "Open app" only when the user has a PM/supplier (operator/supplier) workspace; a customer-only login shows their avatar chip → /customer; logged-out shows Log in / Get started. (2) Property cards equal height — `PropertyCard` root now `h-full flex flex-col` + body `flex-1` so grid rows stretch evenly when figures are missing. (3) Stays guests dropdown was `z-20` → sank under the `sticky z-30` filter bar; bumped to `z-50` (backdrop z-40). (4) Checkout trust badges were a cramped 4-up that wrapped labels → 2×2 grid. | `src/components/marketing/PublicNav.tsx`, `src/components/portfolio/PropertyCard.tsx`, `src/app/stays/StaysHeroSearch.tsx`, `src/features/checkout/components/primitives.tsx` |
| FIX-314 | 2026-06-22 | Upload hardening + image cropper + Recent-Activity 400 (browser-found). (1) `uploadFile` now runs `validateUploadFile` pre-flight (empty/oversized/blocked/wrong-type) so every call site fails fast with a friendly message instead of a server round-trip; image uploaders pass `imagesOnly`. (2) New reusable dependency-free `ImageCropModal` (pan + zoom, aspect-locked, rule-of-thirds, portalled to body, canvas export) wired into image uploads with the correct ratio so they're never squashed: property **cover 16:9**, **avatar 1:1**. Browser-verified end-to-end: cropper opens with a realistic photo at 16:9 → Apply → `POST /api/upload [200]` + `PATCH properties [204]`. (3) Fixed Recent Activity always-400: `useActivityLog` filtered `.in("entity_id", …)` on a SELECT ALIAS — PostgREST needs the real column `resource_id`. (4) Fixed cover not displaying after upload — ROOT CAUSE: `next/image` optimised the authed `/api/files/{key}` URL server-side WITHOUT the session cookie → 401 → broken image → gradient fallback. Fix: `unoptimized` on the cover `<Image>` for `/api/files` srcs so the browser loads it directly (with cookie). Also switched the cover write to `updateProperty.mutateAsync` (cache-invalidating). Browser-verified: cropped realistic photo now renders as the cover end-to-end. Logos display object-contain so intentionally not cropped. NOTE: any other `next/image` showing authed `/api/files` images (property cards, gallery) needs the same `unoptimized` treatment. | `src/lib/upload.ts`, `src/components/upload/ImageCropModal.tsx` (NEW), `src/app/(app)/app/portfolio/properties/[id]/page.tsx`, `src/components/portfolio/property-detail/OverviewTab.tsx`, `src/components/settings/AvatarUploader.tsx`, `src/components/portfolio/property-detail/shared.tsx` |
| FIX-313 | 2026-06-22 | Property Overview reorg + premium maps (browser-found, founder-driven). (1) Layout: hero now fills the left column (was a half-empty stretched "Financial & Occupancy" card → removed, redundant); KPI strip is a uniform `lg:grid-cols-7` grid (was a flex-scroll with inconsistent widths); Financial Summary moved into the right column under Location (killed the bottom-right void); removed a duplicate Financial Summary; Recent Activity now `max-h` internal-scroll. (2) Maps: removed all "View/Open in OpenStreetMap" external links (property Overview + Map tab); `LocationMap` mini-maps are now click-to-enlarge into a premium **pop-over portalled to `<body>`** (was trapped by an ancestor transform → off-centre + overlapping the page) with header + close; enlarged + mini maps **centre on the marker** (single → setView; multi → centre on the selected/clicked marker); geocoding now prefers MapTiler (reliable) with Nominatim fallback and **never persists a null** (a transient failure no longer permanently hides the pin); Map tab is fully interactive (`interactive`, not a mini). Browser-verified. | `src/components/portfolio/property-detail/OverviewTab.tsx`, `src/components/portfolio/property-detail/MapTab.tsx`, `src/components/maps/LocationMap.tsx` |
| FIX-312 | 2026-06-22 | PWA/mobile duplicate header (browser-found). On phones/PWA, pages rendered both `MobileTopBar` (title) AND `MobilePageHeader` (same title) → duplicate heading (e.g. "Jobs / Work orders" then "Jobs / 8 jobs"). Added `hideTitle` prop to `MobilePageHeader` — when set it surfaces the count as the line instead of repeating the title (keeps search + filters). Applied to all 25 pages that pair the two components (work jobs/tasks/suppliers, money ×8, contacts ×6, portfolio properties/units/tenancies/gallery, calendar events, bookings/listings). The 5 solo pages (compliance ×4, planning sets) untouched. Browser-verified at 390×844: single "Jobs" header + "8 jobs" count + search. | `src/components/mobile/MobilePageHeader.tsx` + 25 page files |
| FIX-311 | 2026-06-22 | Live maps everywhere — replaced fake/placeholder maps with real `LocationMap` (MapTiler, geocodes addresses) across the site. Wired: stays detail "Location overview", public provider profile coverage, listings wizard BasicsStep, planning wizard Step02Basics, customer BookingsMapView (multi-marker, status-coloured, click-to-select), supplier Jobs (Active/Day route/selected via `StaticMap` markers), supplier Schedule CalendarTab (week/today/selected via `MapPlaceholder` markers), supplier overview JobsTab + RequestsTab + requests NewTab map views, and Contact profile location. `StaticMap`/`MapPlaceholder` now render a live map when markers are supplied (decorative fallback only when no geodata). Property detail + work-supplier detail already had live maps (unchanged). Unit/tenancy detail deferred (sub-records carry only `property_id`, no address — needs property-address plumbing); team-dispatch route preview deferred (no per-stop address in scope). Build green. | `src/app/stays/[slug]/page.tsx`, `src/app/providers/[slug]/page.tsx`, `src/features/listings/wizard/steps/BasicsStep.tsx`, `src/components/planning/wizard/steps/Step02Basics.tsx`, `src/features/customer/bookings/components/BookingsMapView.tsx`, `src/features/supplier/jobs/components/primitives.tsx` + ActiveTab + ScheduledTab, `src/features/supplier/schedule/components/shared.tsx` + CalendarTab, `src/features/supplier/overview/tabs/JobsTab.tsx` + RequestsTab, `src/features/supplier/requests/components/NewTab.tsx`, `src/components/contacts/contact-detail/ContactProfileTab.tsx` |
| FIX-310 | 2026-06-21 | Map basemap overhaul — colourful + reliable + compliant. Centralised every Leaflet map onto one tile source (`src/lib/maps/tiles.ts`): MapTiler "Outdoor v4" (green topographic, CDN-reliable) via `NEXT_PUBLIC_MAPTILER_KEY`, with an OSM-green fallback if the key is absent. Fixed blank tiles (subdomains were `"abcd"` → OSM has no `d` host → ~25% 404s; now `"abc"`). Removed prominent "Leaflet"/CARTO branding; discreet faded attribution via global CSS + `AttributionControl prefix={false}`, crediting "© MapTiler © OpenStreetMap" (licence-compliant). Applied across all 11 maps: providers/services/stays/emergency marketplace maps, contacts, long-term rentals, portfolio LeafletMap, LocationMap, booking StayMap, public MarketplaceMap, supplier ZoneEditorMap. Also added `https://api.maptiler.com` to CSP `img-src`+`connect-src` in next.config.ts (was blocking every MapTiler tile — marker/attribution rendered but tiles were CSP-blocked = blank grey map). NOTE: add `NEXT_PUBLIC_MAPTILER_KEY` to Vercel env for production; next.config.ts change requires a dev-server restart. | `src/lib/maps/tiles.ts` (NEW), `.env.local`, `next.config.ts`, all map inner components, `src/app/globals.css` |
| FIX-309 | 2026-06-21 | Emergency folded into Services (founder decision — one discovery surface). Removed the standalone "Emergency" hub tab; the marketplace is now Suppliers \| Services. Emergency is an "Emergency" filter/view inside Services: the `emergency` chip (or legacy `?tab=emergency` / `?filters=emergency`) renders the red emergency call-out view (`EmergencyView`) with a "← Back to all services" link; a "Need it urgently?" promo banner in the normal Services view surfaces it. Emergency profile/map/book routes unchanged. | `src/components/marketplace/SuppliersHubTabs.tsx`, `src/app/(app)/app/marketplace/suppliers-hub/page.tsx`, `src/app/(app)/app/marketplace/suppliers-hub/sections.tsx` |
| FIX-308 | 2026-06-21 | Real media + consistent premium map treatment. (1) Deterministic Unsplash media fallbacks (`media-fallback.ts`) — trade/category-relevant hero photos + portrait avatars applied to ALL providers/services/emergency at the query layer, so live rows with no uploaded image (e.g. "Demo Trades") show real pictures + avatars instead of a gradient initial. (2) Compacted the suppliers popup (232px, 60px hero, premium blue-tinted outline) and fixed white button-label text being clobbered by Leaflet's `.leaflet-container a` colour rule (labels now in coloured `<span>`s). (3) Map pins are premium blue gradient. (4) Coverage rings now render ONLY for the open marker (was every ring at once = messy). (5) Same premium popup + auto-fit-bounds applied to the Services and Stays maps for consistency. | `src/lib/public-marketplace/media-fallback.ts` (NEW), `src/lib/public-marketplace/queries.ts`, `src/components/public-marketplace/maps/ProvidersMapInner.tsx`, `src/components/public-marketplace/maps/ServicesMapInner.tsx`, `src/components/public-marketplace/maps/StaysMapInner.tsx`, `src/app/globals.css` |
| FIX-307 | 2026-06-21 | Premium map popup + left-rail cards. On-map Leaflet popup rebuilt as a full premium card — flush hero image (`propvora-map-popup` CSS zeroes Leaflet's default popup chrome: padding, 16px radius, soft shadow, pill close button), featured/24-7 overlay badges, trade pill, rating, location, response, vetted/insured/Gas Safe/NICEIC certification chips, From £X price, and dual CTAs (View profile + Book now). Left-rail `ProviderMapCard` upgraded to premium: hero thumbnail with scrim + trade pill + featured star, hover lift/shadow/scale, cert chips row, price + reveal-on-hover "View ›". | `src/components/public-marketplace/maps/ProvidersMapInner.tsx`, `src/components/public-marketplace/cards/ProviderMapCard.tsx`, `src/app/globals.css` |
| FIX-306 | 2026-06-21 | Suppliers marketplace + map polish. (1) Removed the bottom "Why choose verified suppliers?" cards + mismatched "Verified stays / 24/7 guest support" trust strip from the Suppliers tab. (2) Map view left rail: replaced the oversized image `ProviderCard` with a new compact horizontal `ProviderMapCard` (thumbnail + details, wired save), added a rail header + empty state, widened to 380px. (3) Map now auto-fits to the provider markers on load (`FitToProviders` via Leaflet `fitBounds`) instead of opening on a fixed Manchester centre with an off-screen result. (4) Area chips were hardcoded Manchester neighbourhoods with no effect — made them DATA-DRIVEN (distinct cities from the result set, most-common first) and wired to `?where=` so they actually filter the list + map; map page now filters providers by the active area. | `src/app/(app)/app/marketplace/suppliers-hub/sections.tsx`, `src/components/public-marketplace/cards/ProviderMapCard.tsx` (NEW), `src/app/(app)/app/marketplace/suppliers-hub/map/page.tsx`, `src/components/public-marketplace/maps/ProvidersMapInner.tsx`, `src/components/public-marketplace/maps/MapAreaChips.tsx` |
| FIX-305 | 2026-06-21 | Suppliers nav = marketplace only (founder decision). The `marketplaceEnabled`-flagged "Suppliers" sidebar item landed on `/property-manager/suppliers`, which rendered the internal supplier-network hub (Overview/Directory/Compliance/Performance) instead of the rebuilt marketplace. Retired the internal hub: index + all three sub-routes (`/suppliers`, `/suppliers/directory`, `/suppliers/compliance`, `/suppliers/performance`) now redirect to `/property-manager/marketplace/suppliers-hub` (unified Suppliers \| Services \| Emergency marketplace, 1:1 with public `/services`). | `src/app/(app)/app/suppliers/page.tsx`, `src/app/(app)/app/suppliers/directory/page.tsx`, `src/app/(app)/app/suppliers/compliance/page.tsx`, `src/app/(app)/app/suppliers/performance/page.tsx` |
| FIX-302 | 2026-06-21 | Sidebar Suppliers nav — wrong route. SideNavigation "Suppliers" item pointed to `/property-manager/marketplace/suppliers-hub` (public marketplace search) instead of `/property-manager/suppliers` (PM's own supplier network hub with directory/compliance/performance tabs). Changed href to `/property-manager/suppliers`. SuppliersHubTabNav already uses correct `/property-manager/suppliers/*` sub-paths. | `src/components/shell/SideNavigation.tsx` |
| FIX-301 | 2026-06-21 | /marketplace route deleted — (marketplace-public)/marketplace/ route group removed entirely; /stays, /services, /suppliers are the canonical public marketplace routes. Added permanent redirects in next.config.ts: /marketplace/stays→/stays, /marketplace/suppliers→/suppliers, /marketplace/services→/services, /marketplace/*→/services. Also cleaned up old partial /marketplace/stays etc. redirects that were already there. | `src/app/(marketplace-public)/` (DELETED), `next.config.ts` |
| FIX-300 | 2026-06-21 | Automations HomePage crash (P1) — AutomationsDataTable threw "TypeError: Cannot read properties of undefined (reading 'split')" when an automation row had undefined owner. Fixed with null guard: `(r.owner ?? "").split(" ").filter(Boolean).map(...)` | `src/features/automations/pages/HomePage.tsx` |
| FIX-299 | 2026-06-21 | Marketplace P0 crash — /marketplace threw "Invalid src prop (gradient://red) on next/image" from a DB listing with a non-HTTP thumbnail_url, crashing the page inside ErrorBoundaryHandler. Fixed: `safeImageUrl()` validator in `rpcRowToPublic` returns null for any URL that doesn't start with http:// or https://, falling back to the card's gradient placeholder. | `src/lib/marketplace/search.ts` |
| FIX-298 | 2026-06-21 | AI copilot improvements — (1) system prompt: 4 new guidelines to never echo workspace context block, give brief greetings, only mention counts when relevant; (2) chat persistence: threadId saved to localStorage + restored on mount via GET /api/ai/threads/[id]; (3) multi-thread: New button + /clear command both call handleNewChat() to clear localStorage + reset state; (4) two new API routes: GET /api/ai/threads (list 20 threads) + GET /api/ai/threads/[id] (load messages, user-scoped RLS). tsc EXIT:0. | `src/app/api/ai/chat/route.ts`, `src/features/copilot/screens/CopilotChatScreen.tsx`, `src/app/api/ai/threads/route.ts` (NEW), `src/app/api/ai/threads/[id]/route.ts` (NEW) |
| FIX-297 | 2026-06-21 | globals.css PostCSS worker timeout (P0) — dev server and prod build crashed with TurbopackInternalError (timeout while receiving message from process) because @tailwindcss/postcss v4 auto-scanned 725+ pages. Fixed: `@import "tailwindcss" source(none)` disables auto-scan; `@source "../**/*.{ts,tsx,js,jsx,html}"` scans only src/. Also removed unused `@variant dark` (zero dark: class usage confirmed). Dev server now compiles in 54s cold cache. | `src/app/globals.css` |
| FIX-296 | 2026-06-21 | PM workspace nav de-bloat — (1) Affiliate nav item flag-gated behind new `affiliateEnabled` flag (hidden by default, enabled per workspace on enrolment); (2) Legal merged as 9th tab inside ComplianceTabNav (links to /app/legal, active-detects /app/legal/*) and removed from OPERATIONS sidebar group — sidebar drops from 14→12 visible items in V1; (3) `affiliateEnabled` flag added to registry.ts (24th flag) and meta.ts; (4) FIX-293 post-merge: CopilotConversationView missing `conversationId?` prop fixed (TS2322). | `src/components/shell/SideNavigation.tsx`, `src/components/compliance/ComplianceTabNav.tsx`, `src/lib/flags/registry.ts`, `src/lib/flags/meta.ts`, `src/features/copilot/screens/CopilotConversationView.tsx` |
| FIX-293 | 2026-06-21 | CSS design tokens â€” add CSS custom properties to globals.css (`--bg-app-shell`, `--bg-marketing-dark`, `--bg-map-placeholder`, `--color-primary/accent/success/warning/error` + light variants + `--color-surface-sunken`). Add `bg-app-shell` and `bg-marketing-dark` utility classes. Migrate 70+ inline `style=` hex refs to CSS vars across 5 shell components, 4 copilot components, 8 marketing components, 4 auth pages, admin-login, 30+ planning/contacts/money/calendar/account/workspace-settings pages. DESIGN-026 score 2â†’4. | `src/app/globals.css`, `src/components/shell/AppShell.tsx`, `src/components/shells/AdminShell.tsx`, `src/components/shells/CustomerShell.tsx`, `src/components/shells/PortalShell.tsx`, `src/components/shells/SupplierAppShell.tsx`, `src/components/shell/ShellTabsRail.tsx`, `src/features/copilot/components/CopilotPanelShell.tsx`, `src/features/copilot/components/CopilotMessageBubble.tsx`, `src/features/copilot/components/CopilotUpgradePrompt.tsx`, `src/features/copilot/screens/CopilotConversationView.tsx`, `src/guided-help/components/FirstUseModal.tsx`, `src/components/pwa/InstallPrompt.tsx`, `src/components/maps/LocationMap.tsx`, `src/components/portfolio/LeafletMap.tsx`, `src/components/portfolio/unit-detail/UnitOverviewTab.tsx`, multiple marketing/landing/suppliers components, auth pages, planning pages, contacts pages, money pages, account pages, workspace-settings pages, calendar pages, `qa-release/sections/19-design-consistency.md` |
| FIX-294 | 2026-06-21 | Feature flag enforcement â€” AutomationsTabs now accepts hiddenTabs prop; canvas/page.tsx and home/page.tsx resolve canvasLite+automationsFull server-side and hide Canvas Builder tab + Webhooks/Integrations tabs when OFF; AutomationsClient and HomePage hide Canvas button when canvasEnabled=false; FLAG-001-023 all scored in section 13; 17 of 23 flags now CODE_CONFIRMED | `src/features/automations/components/AutomationsTabs.tsx`, `src/features/automations/components/AutomationsModuleShell.tsx`, `src/app/(app)/app/automations/home/page.tsx`, `src/features/automations/pages/HomePage.tsx`, `src/app/(app)/app/automations/canvas/page.tsx`, `src/app/(app)/app/automations/AutomationsClient.tsx`, `qa-release/sections/13-feature-flags.md`, `qa-release/implementation-fix-log.md` |
| FIX-295 | 2026-06-21 | Security hardening â€” CSRF helper (`checkCsrf` + `stripHtml`) in new `src/app/api/_shared.ts`; XSS audit (5 `dangerouslySetInnerHTML` sites confirmed safe â€” server-sanitised or static strings); IDOR pattern verified across automations/runs + ical/connections + upload (all session-derived); HTTP security headers confirmed all 7 present in `next.config.ts`; auth audit log wired â€” `AUDIT_ACTIONS.AUTH_LOGIN/AUTH_LOGOUT` added, auth callback logs on successful OAuth; UPLOAD-016 blocked-extension check (`hasBlockedExtension` + `hasBlockedMimeType`) added to client validation pipeline in `src/lib/upload.ts`; SEC+UPLOAD all scored 5/5 | `src/app/api/_shared.ts` (NEW), `src/app/api/auth/callback/route.ts`, `src/lib/audit/log.ts`, `src/lib/upload.ts`, `qa-release/sections/01-pm-workspace.md`, `qa-release/sections/08-admin.md`, `qa-release/implementation-fix-log.md` |
| FIX-292 | 2026-06-21 | QA audit â€” FLAG/DESIGN/PWA/SEC/UPLOAD sections scored; /help and /clear AI commands fixed client-side (no API call); merge conflict in section 14 resolved; SEC-010/019/020/021/023 code-confirmed; UPLOAD-011/012/013/018 code-confirmed; DESIGN-026 hardcoded hex noted (V2 refactor); DESIGN-027 zero dark: confirmed; PWA manifest 5/5 confirmed; PwaActionBar clarified as MobileBottomNav/CustomerMobileNav/SupplierMobileNav | `src/features/copilot/screens/CopilotChatScreen.tsx`, `qa-release/sections/01-pm-workspace.md`, `qa-release/sections/13-feature-flags.md`, `qa-release/sections/14-ai-copilot.md`, `qa-release/sections/18-pwa-mobile.md`, `qa-release/sections/19-design-consistency.md`, `qa-release/master-scoreboard.md`, `qa-release/implementation-fix-log.md` |
| FIX-291 | 2026-06-21 | i18n 100/100 â€” currency hooks, date formatter, jurisdiction footer notes, AI jurisdiction clause, AuthProvider settings wiring, formatPence locale param | `src/providers/AuthProvider.tsx`, `src/hooks/useWorkspaceJurisdiction.ts` (NEW), `src/hooks/useFormatCurrency.ts` (NEW), `src/hooks/useFormatDate.ts` (NEW), `src/lib/marketplace/money.ts` (formatPence locale param), `src/lib/international/countries.ts` (aiJurisdictionClause NEW), `src/app/(app)/app/legal/layout.tsx` (jurisdiction footer), `src/app/(app)/app/compliance/layout.tsx` (jurisdiction footer) |
| FIX-287 | 2026-06-21 | Enterprise i18n Supabase hardening â€” workspace.settings wired end-to-end, saveI18nPreferences server action, preferences page, useWorkspaceJurisdiction + useFormatCurrency hooks, count badges on ComplianceTabNav/MoneyTabNav/LegalTabNav | `src/providers/AuthProvider.tsx`, `src/app/(app)/app/workspace-settings/preferences/page.tsx` (NEW), `src/app/(app)/app/workspace-settings/preferences/actions.ts` (NEW), `src/app/(app)/app/workspace-settings/layout.tsx`, `src/hooks/useWorkspaceJurisdiction.ts` (NEW), `src/hooks/useFormatCurrency.ts` (NEW), `src/components/compliance/ComplianceTabNav.tsx`, `src/components/money/MoneyTabNav.tsx`, `src/components/legal/LegalTabNav.tsx` |
| FIX-286 | 2026-06-21 | Country-specific tab systems â€” compliance/money/portfolio/legal tabs per jurisdiction (GB/US/AU/CA/DE/AE), jurisdiction banner, CountryTabs component, terminology adaptation | `src/lib/i18n/tab-config.ts`, `src/lib/i18n/use-country-tabs.ts`, `src/hooks/useWorkspaceJurisdiction.ts`, `src/components/i18n/JurisdictionBanner.tsx`, `src/components/i18n/CountryTabs.tsx`, `src/components/compliance/ComplianceTabNav.tsx`, `src/components/money/MoneyTabNav.tsx`, `src/components/legal/LegalTabNav.tsx`, `src/app/(app)/app/compliance/layout.tsx`, `src/app/(app)/app/legal/layout.tsx`, `src/app/(app)/app/money/page.tsx`, `src/app/(app)/app/portfolio/properties/[id]/page.tsx` |

