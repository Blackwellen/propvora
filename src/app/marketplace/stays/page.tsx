import PublicMarketplacePage from "../_components/PublicMarketplacePage"

export const metadata = {
  title: "Marketplace Stays | Propvora",
  description: "Discover managed stays and direct booking pages.",
}

export default function MarketplaceStaysPage() {
  return <PublicMarketplacePage kind="stays" />
}
